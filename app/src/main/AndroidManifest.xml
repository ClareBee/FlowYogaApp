<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clareblackburne.yogaapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LaunchPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PoseActivity" />
        <activity android:name=".AddPoseActivity" />
        <activity android:name=".TopPosesActivity" />

        <activity android:name=".SessionActivity" />
        <activity android:name=".AddSessionActivity" />
        <activity android:name=".TopSessionsActivity" />

        <activity android:name= ".QuotationActivity" />
        <activity android:name=".AddSetActivity" />

    </application>

</manifest>